<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Logout - LiveKit Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
</head>
<body data-bs-theme="dark" class="d-flex align-items-center justify-content-center min-vh-100">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-6">
                <div class="card">
                    <div class="card-body text-center py-5">
                        <i class="bi bi-check-circle text-success" style="font-size: 4rem;"></i>
                        <h1 class="mt-3">Logged Out</h1>
                        <p class="lead">You have been successfully logged out.</p>
                        <p class="text-muted">
                            Your session has been cleared and server selection reset.
                        </p>
                        <div class="mt-4">
                            <a href="/login" class="btn btn-primary me-2">
                                <i class="bi bi-box-arrow-in-right me-2"></i>
                                Login Again
                            </a>
                            <button onclick="window.close()" class="btn btn-secondary">
                                <i class="bi bi-x-circle me-2"></i>
                                Close Tab
                            </button>
                        </div>
                        <div class="mt-3">
                            <small class="text-muted">
                                <i class="bi bi-info-circle me-1"></i>
                                You will need to select your LiveKit server again when you log back in.
                            </small>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script>
        // Auto-redirect to login after 3 seconds
        setTimeout(() => {
            window.location.href = '/login';
        }, 3000);
    </script>
</body>
</html>

